import 'package:flutter/material.dart';

class Sponsorships extends StatelessWidget {
  const Sponsorships({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Sponsorships")),
      body: const Center(child: Text("Sponsorships Page")),
    );
  }
}